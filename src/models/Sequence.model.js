export default class Sequence {
  constructor(minimalValue, signature, options, barsList) {
    this.minimalValue = minimalValue;
    this.signature = signature;
    this.options = options;
    this.barsList = barsList;
  }
}
